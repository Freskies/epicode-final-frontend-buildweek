import "./dropdown.css"

const DropdownContent = ({children}) => {
    return (
        <div className="dropdown-content">
            {children}
        </div>
    )
}

export default DropdownContent
